name: zejtron - AUR Sync

on:
  repository_dispatch:
    types:
      - aur-sync
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag, such as v0.2.0 or 0.2.0
        required: true
        type: string

permissions:
  contents: read

env:
  AUR_REPO: ssh://aur@aur.archlinux.org/zejtron-bin.git
  AUR_BRANCH: master
  PKGBUILD_DIR: aur/zejtron-bin

jobs:
  sync:
    name: Sync zejtron-bin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve release tag
        id: version
        shell: bash
        env:
          RAW_TAG: ${{ github.event_name == 'repository_dispatch' && github.event.client_payload.tag || inputs.tag }}
          AUR_SSH_PRIVATE_KEY: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
        run: |
          set -Eeuo pipefail
          echo "event name: ${GITHUB_EVENT_NAME}"
          echo "raw tag: ${RAW_TAG}"
          if [[ -n "${AUR_SSH_PRIVATE_KEY}" ]]; then
            echo "AUR_SSH_PRIVATE_KEY present: yes"
          else
            echo "AUR_SSH_PRIVATE_KEY present: no"
          fi

          tag="$RAW_TAG"
          if [[ "$tag" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            tag="v${tag}"
          fi
          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "invalid tag: $RAW_TAG" >&2
            exit 1
          fi
          pkgver="${tag#v}"
          echo "resolved tag: ${tag}"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "pkgver=$pkgver" >> "$GITHUB_OUTPUT"

      - name: Update package metadata
        shell: bash
        run: |
          set -Eeuo pipefail
          pkgver="${{ steps.version.outputs.pkgver }}"
          sed -i -E "s/^pkgver=.*/pkgver=${pkgver}/" "${PKGBUILD_DIR}/PKGBUILD"
          sed -i -E "s/^pkgrel=.*/pkgrel=1/" "${PKGBUILD_DIR}/PKGBUILD"
          {
            printf 'pkgbase = zejtron-bin\n'
            printf '\tpkgdesc = Small Linux terminal toolkit for tracing paths, ports, env, recent files, and services\n'
            printf '\tpkgver = %s\n' "$pkgver"
            printf '\tpkgrel = 1\n'
            printf '\turl = https://github.com/oxyzenQ/zejtron\n'
            printf '\tarch = x86_64\n'
            printf '\tarch = aarch64\n'
            printf '\tlicense = MIT\n'
            printf '\tdepends = glibc\n'
            printf '\tdepends = gcc-libs\n'
            printf '\tprovides = zejtron\n'
            printf '\tconflicts = zejtron\n'
            printf '\toptions = !strip\n'
            printf '\n'
            printf 'pkgname = zejtron-bin\n'
          } > "${PKGBUILD_DIR}/.SRCINFO"

      - name: Setup SSH
        shell: bash
        env:
          AUR_SSH_PRIVATE_KEY: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
        run: |
          set -Eeuo pipefail
          if [[ -z "${AUR_SSH_PRIVATE_KEY}" ]]; then
            echo "AUR_SSH_PRIVATE_KEY secret is required" >&2
            exit 1
          fi

          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$AUR_SSH_PRIVATE_KEY" > ~/.ssh/aur
          chmod 600 ~/.ssh/aur

          keyscan="$(ssh-keyscan -t ed25519 aur.archlinux.org 2>/dev/null)"
          if [[ -z "$keyscan" ]]; then
            echo "failed to scan AUR SSH host key" >&2
            exit 1
          fi
          fingerprint="$(printf '%s\n' "$keyscan" | ssh-keygen -lf - | awk '{print $2}')"
          expected="SHA256:RFzBCUItH9LZS0cKB5UE6ceAYhBD5C8GeOBip8Z11+4"
          if [[ "$fingerprint" != "$expected" ]]; then
            echo "AUR SSH host fingerprint mismatch" >&2
            echo "expected: $expected" >&2
            echo "actual:   $fingerprint" >&2
            exit 1
          fi
          printf '%s\n' "$keyscan" > ~/.ssh/known_hosts
          cat > ~/.ssh/config <<'EOF'
          Host aur.archlinux.org
            User aur
            IdentityFile ~/.ssh/aur
            IdentitiesOnly yes
            StrictHostKeyChecking yes
          EOF
          chmod 600 ~/.ssh/config ~/.ssh/known_hosts

      - name: Clone AUR repository
        shell: bash
        run: |
          set -Eeuo pipefail
          set +e
          clone_output="$(git clone --branch "${AUR_BRANCH}" "${AUR_REPO}" aur-repo 2>&1)"
          clone_status=$?
          set -e

          if [[ "$clone_status" -eq 0 ]]; then
            exit 0
          fi

          printf '%s\n' "$clone_output"

          if grep -Eqi 'permission denied|publickey|authentication|host key|identification has changed' <<<"$clone_output"; then
            echo "failed to authenticate or verify AUR SSH host" >&2
            exit "$clone_status"
          fi

          if grep -Eqi 'not found|does not appear to be a git repository|could not read from remote repository|remote branch .* not found|empty repository' <<<"$clone_output"; then
            echo "AUR repository appears empty or not initialized; bootstrapping local repository"
            mkdir aur-repo
            cd aur-repo
            git init
            git checkout -B "${AUR_BRANCH}"
            git remote add origin "${AUR_REPO}"
            exit 0
          fi

          echo "failed to clone AUR repository" >&2
          exit "$clone_status"

      - name: Copy package files
        run: |
          cp "${PKGBUILD_DIR}/PKGBUILD" aur-repo/PKGBUILD
          cp "${PKGBUILD_DIR}/.SRCINFO" aur-repo/.SRCINFO

      - name: Commit AUR update
        working-directory: aur-repo
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name "rezky_nightky"
          git config user.email "rezky2399@proton.me"
          git add PKGBUILD .SRCINFO
          if git diff --cached --quiet; then
            echo "AUR package is already up to date"
            exit 0
          fi
          git commit -m "Update zejtron-bin to ${{ steps.version.outputs.tag }}"

      - name: Push AUR update
        working-directory: aur-repo
        shell: bash
        run: |
          set -Eeuo pipefail
          for attempt in 1 2 3; do
            if git push origin "${AUR_BRANCH}"; then
              exit 0
            fi
            sleep $((attempt * 5))
            git pull --rebase origin "${AUR_BRANCH}"
          done
          git push origin "${AUR_BRANCH}"

      - name: Cleanup SSH key
        if: always()
        run: rm -f ~/.ssh/aur
